---
"@vercel/agent-eval": patch
---

Capture transcripts on a best-effort basis for failed and aborted runs (not just successful runs) across supported agents, so result folders can include transcripts when available for downstream ingestion and debugging.
